name: Dependency maintenance

on:
  schedule:
    - cron: "17 6 * * 2"
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  update:
    name: Propose policy-approved updates
    uses: rvben/upd/.github/workflows/dependency-health.yml@be91f2e854b0c6386f253175db02a0e585d9244a # v0.8.4
    with:
      langs: rust,actions
      lock: true
      min-age: 7d
      max-bump: minor
      update-action-shas: true
      prepare-command: rustup toolchain install 1.87.0 --profile minimal --component clippy,rustfmt && rustup default 1.87.0
      validation-command: make fmt-check check lint test docs-check
      fail-on-blocked: true
      branch: automation/upd-dependencies
      commit-message: "chore(deps): update dependencies with upd"
      auto-merge: false
      app-client-id: ${{ vars.UPD_APP_CLIENT_ID }}
    secrets:
      app-private-key: ${{ secrets.UPD_APP_PRIVATE_KEY }}
